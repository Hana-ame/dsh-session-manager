{
  "name": "@local/dsh-session-manager",
  "version": "0.3.0",
  "private": true,
  "description": "Session manager: one package holding the durable per-session store (relations + describe notes) and the management tools that read and write it.",
  "type": "module",
  "exports": {
    "./tools": "./lib/tools.mjs",
    "./store": "./lib/store.mjs",
    "./package.json": "./package.json"
  }
}
